# gateset-shadow-single-qubit-qolab
#
# 1. put 'GSS.py' in ~/code/calibration_graph 
# 2. use the data in 'data' folder
# 3. run 'GSS_RB_git_tools.ipynb' or 'GSS_IRB_git_tools_up-x180.ipynb' with loading the data in 'data' folder

# --> output : 1) the decay graph of GSS, 2) the decay parameter p_GSS  
